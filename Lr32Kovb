import java.util.*;

public class Lr32Kovb {
    
    public static void addValue(HashSet<String> hashSet, String value){
        hashSet.add(value);
    }

    public static void isEmpty(HashSet<String> hashSet){
        if (hashSet.isEmpty()) {
            System.out.println("HashSet is empty.");
        } else {
            System.out.println("HashSet is not empty.");
        }
    }
    
    
    public static void main(String[] args) {
        HashSet<String> hashSet = new HashSet<>();

        addValue(hashSet,"One");
        addValue(hashSet,"Two");
        addValue(hashSet,"Tree");
        isEmpty(hashSet);
    }
}
